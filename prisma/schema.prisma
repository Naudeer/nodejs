datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model People {
  id    Int    @id @default(autoincrement())
  name  String
  age   Int
}